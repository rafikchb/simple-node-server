name: continius-delivry
on:
    push:
        branches: ["live"]

jobs:
    deploy:
        runs-on: self-hosted
        steps:
            - uses: actions/checkout@v3
            - run: echo "hello"
            - run: npm install
            - run: npm run build --if-present
            - run: npm test